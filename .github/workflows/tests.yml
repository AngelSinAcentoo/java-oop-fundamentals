name: tests

on:
  push:
  pull_request:

jobs:
  java:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - shell: pwsh
        run: ./run-tests.ps1
